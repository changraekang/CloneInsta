import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EdittProfile = () => {
  return (
    <View>
      <Text>EdittProfile</Text>
    </View>
  );
};

export default EdittProfile;

const styles = StyleSheet.create({});
